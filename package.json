{
  "name": "upload",
  "version": "1.0.0",
  "description": "A microservice for uploading images to an S3 bucket using signed URLs",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Keith Harris",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.814.0",
    "fs": "0.0.1-security",
    "uuid": "^3.4.0"
  }
}
